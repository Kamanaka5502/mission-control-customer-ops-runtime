APP_ENV=production
APP_MODE=customer-operations-platform
DATABASE_URL=postgresql+psycopg://mission_control:change_me@postgres:5432/mission_control
REQUIRE_TENANT_HEADER=true
NEXT_PUBLIC_API_BASE=https://api.example.com

# Deploy behind authenticated ingress. Do not rely on demo headers as identity proof.
# x-actor-role and x-tenant-id must be injected by trusted middleware or gateway.
